# tableau-sql
une rerquête pour ajouter un film: INSERT INTO films(titre, date_de_sortie, durée, réalisateur, cathegories_des_films_idcategories) VALUES ('', '' ,'', '','');
une requête pour modifier un film :UPDATE films titre= '', date_de_sortie = '', durée= '', réalisateur = '' cathegories_des_films_idcategories = 1;
une requête pour supprimer un film : DELETE FROM films WHERE cathegories_des_films_idcategories = 1;
une requête pour ajouter un client : INSERT INTO clients (idclients, NOM, prenom, email VALUES ('', '', '', '');
une requête pour modifier un client : UPDATE clients SET idclients = NULL, NOM = '', prenom= '', email = ''WHERE cathegories_des_films_idcategories = 1;
une requête pour supprimer un client :DELETE FROM clients WHERE clients_idclients = 1;
une requête pour afficher les 3 derniers films ajoutés :SELECT * FROM films ORDER BY idfilms DESC LIMIT 3;
